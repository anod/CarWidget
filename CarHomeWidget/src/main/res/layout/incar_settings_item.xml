<?xml version="1.0" encoding="utf-8"?>

<Button xmlns:android="http://schemas.android.com/apk/res/android"
		android:drawableTop="@drawable/ic_add_shortcut_holo"
		android:layout_width="@dimen/incar_item_width"
		android:layout_height="@dimen/incar_item_height"
		android:text="Very long piece of text"
		android:textSize="12sp"
		android:drawablePadding="0dp"
		android:paddingTop="10dp"
		android:paddingBottom="5dp"
		android:paddingLeft="2dp"
		android:paddingRight="2dp"
		android:ellipsize="end"
		android:lines="2"
		android:id="@+id/button"
		android:background="@drawable/incar_item_bg_grey"
		/>